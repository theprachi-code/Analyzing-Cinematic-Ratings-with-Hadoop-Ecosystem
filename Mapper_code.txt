#!/usr/bin/env python
import sys

for line in sys.stdin:
    if "userId" in line:
        continue
    
    data = line.strip().split(',')
    movie = data[2]
    rating = float(data[4])
    print("%s\t%s" % (movie, rating))
